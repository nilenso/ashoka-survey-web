<div>
  <label><%= t("activerecord.attributes.survey.name") %></label>
  <input type="text" name="name" value="{{name}}"></input>
</div>

<div>
  <label><%= t("activerecord.attributes.survey.expiry_date") %> </label>
  <input id="expiry_date" type="text" name="expiry_date" value="{{expiry_date}}"></input>
</div>

<div>
  <label><%= t("activerecord.attributes.survey.description") %></label>
  <input type="text" name="description" value="{{description}}"></input>
</div>

<div>
  <label><%= t("activerecord.attributes.survey.crowd_source") %></label>
  {{#public}}
    <input type="checkbox" name="public" value="{{public}}" checked ></input>
  {{/public}}
  {{^public}}
    <input type="checkbox" name="public" value="{{public}}"></input>
  {{/public}}
</div>
