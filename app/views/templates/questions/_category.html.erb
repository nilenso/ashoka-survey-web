<div>
  <label><%= t("surveys.build.category_title") %> </label>
  <input type="text" name="content" value="{{content}}"></input>
</div>

{{#id}}
  <div>
    <%= render 'templates/question_types_dropdown' %>
    <button class='add_sub_question' href='#'>Add Sub-Question</button>  
  </div>

  <div>
    <button class='add_sub_category' href='#'>Add Sub-Category</button>
  </div>
{{/id}}
{{^id}}
  <%= render 'templates/loading' %>
{{/id}}